
#include "main.h"


/**

 * mainr - A cde that check if chat is lower
 * 
 * Return: 0:
 */

int _islower(int c)
{
        if (c >= 'a' && c <= 'z')
                return (1);
        else
                return (0);
}
